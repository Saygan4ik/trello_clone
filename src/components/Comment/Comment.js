import React from 'react'

class Comment extends React.Component {
  render() {
    const { author, text, createdAt } = this.props.data;

    return (
      "zxc"
    );
  }
}

export default Comment;
